<!DOCTYPE html>
<html>
  <body>
    <h1>Thank you for your order <%= @user.name %>!</h1>
    <p>
      You have successfully placed Order #<%= @order.id %>
    </p>

    <p> You have ordered the following items:
      <tbody>
        <% @order.line_items.each do |line_items| %><%= render 'orders/maileritem', line_items: line_items %>
        <% end %>
      </tbody>
        <p>TOTAL: <%= humanized_money_with_symbol @order.total %>
  </body>
</html>